---
export type Props = {
  isCenter?: boolean
}

const { isCenter = false } = Astro.props as Props

const centerStyle = isCenter ? 'text-center' : ''
---

<div class={`w-full border-b border-back-dark p-4 dark:border-white md:px-4 ${centerStyle}`}>
  <slot />
</div>
